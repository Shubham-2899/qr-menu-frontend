import { Box, Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Box sx={{ mt: { sm: 8 } }}>
        <Typography variant="h4">Welcome to Admin Panel</Typography>
      </Box>
    </div>
  );
};

export default Home;
